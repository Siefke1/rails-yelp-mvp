<div class="container">
  <h1>All Restaurants</h1>
  <ul>
  <% @restaurants.each do |rest| %>
    <li><%= link_to rest.name, restaurant_path(rest) %></li>
  <% end %>
  </ul>
  <br>
  <%= link_to '-> Create a restaurant <-', new_restaurant_path %>
</div>
